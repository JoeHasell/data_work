# Project Background

## Overview
In this project, I am trying to understand the extent and nature of global income inequality based on percentile/quantile data from two different sources: PIP and WID.

The two sources paint different pictures. In particular, published analyses of these sources differ in one important regard: PIP says that the majority of global inequality is due to differences in the average income between countries ('between-country inequality'); WID says that the majority is due to income differences between rich and poor within countries ('within-country inequality'). Because the concepts, methods and raw data used by each source vary in many different ways, it's difficult to know what explains this key difference between PIP and WID. This project is trying to get to the bottom of that.

To do so, it will use the Mean Log Deviation measure of inequality, which is decomposable into between and within country components.

It will create synthetic/alternative distributions based on the original PIP/WID data to explore what the most important factors might be driving this key difference in takeaways concerning global inequality.

## PIP data
The PIP percentile data can be downloaded here:

"https://catalog.ourworldindata.org/garden/wb/2025-10-13/thousand_bins_distribution/thousand_bins_distribution.feather?nocache"

This contains 1000 income 'bins' per country year, each containing 0.1% of the population — as specified in the 'quantile' column.

The 'avg' column gives the average income within that quantile, measured in 2021 international dollars (daily).

The 'pop' column gives the number of people in that quantile.

**IMPORTANT: PIP data is per capita** - income is divided by the total population (adults + children).

## WID data
The WID data is available in the `inputs/` folder.

The structure is different to the PIP data.

Firstly the data is only for a single year, 2023.

Secondly, there are roughly 100 quantile bins — as specified in 'percentile'. 'p0p1' means percentile 1 (the lowest). The exception is that there is higher resolution at the very top of the distribution. (i.e. not all bins are equal sized.)

**CRITICAL DISTINCTION: WID data is per adult** - The 'avg' values represent average income per adult, not per capita. WID takes household income (typically generated by adults) and divides by the number of adults, not total population. To create comparable per capita figures, you need to multiply by adult_pop and divide by total_pop.

The data is spread out across different files:

- **WID_percentiles.csv** (`inputs/WID_percentiles.csv`): Main percentile income data with columns for country, percentile, year, avg income (per adult, in local currency, annual), share, p_low, p_high

- **country_mapping.csv** (`inputs/country_mapping.csv`): Maps WID two-letter country codes to PIP country names

- **WID_aggregate_population.csv** (`inputs/WID_aggregate_population.csv`): Aggregate population data for each country by year. Contains both:
  - **adult_pop**: Adult population (use this for WID per adult calculations)
  - **total_pop**: Total population including children (use this for per capita calculations)

- **WID_ppp.csv** (`inputs/WID_ppp.csv`): PPP conversion factors to convert local currencies to international dollars

The WID income data is given in local currencies (annual) and needs to be:
1. Converted to international dollars using the PPP factors
2. Converted from annual to daily (divide by 365) to match PIP
3. For per capita comparisons: adjusted from per adult to per capita using (avg_per_adult * adult_pop / total_pop)

- **fetch_percentiles_aggs_and_pop.do** (`inputs/fetch_percentiles_aggs_and_pop.do`): This file is just kept here for reference. It is a STATA file which I run (in a different IDE) in order to get the WID data files just described above. It can be ignored.


## Folder Structure

- **inputs/** - Raw data files (WID files are stored here, the PIP data is retreived from a URL as explained above)
- **modified/** - Intermediate processed data files (e.g., harmonized datasets)
- **outputs/** - Final outputs (charts, tables, analysis results)

## Session History

### Session 1 (Feb 6, 2026) - Data Harmonization and Initial Analysis

**Files Created:**
1. `harmonize_percentiles.py` - Main data harmonization script
2. `analyze_inequality.py` - MLD inequality decomposition analysis
3. `pip_wid_harmonized.csv` - Combined harmonized dataset (2.7MB, 45,126 rows)

**Data Harmonization Accomplished:**
- Created script that downloads and processes both PIP and WID data
- Aggregated PIP data from 1000 bins (0.1% each) to 101 bins matching WID structure:
  - P0-P1, P1-P2, ..., P98-P99 (99 bins of 1% each)
  - P99-P99.9 (0.9% of population)
  - P99.9-P100 (top 0.1%)
- Filtered both datasets to 2023 only (WID only has 2023, PIP has multiple years)
- Unified column structure: `country, year, p_low, p_high, pop, average, source`
  - `p_low` and `p_high` are fractions (0-1) indicating percentile boundaries
  - `average` is mean income in 2021 international dollars
  - `source` distinguishes PIP vs WID data
- WID processing includes:
  - Country code mapping to PIP country names
  - Population calculation for each percentile bin
  - Currency conversion from local currency to international dollars using PPP factors

**Dataset Statistics:**
- PIP: 22,018 rows covering 218 countries for 2023
- WID: 23,108 rows covering 211 countries for 2023
- Both use population-weighted averages for income calculations

**Analysis Setup:**
- Created `analyze_inequality.py` to calculate Mean Log Deviation (MLD)
- Decomposes inequality into between-country and within-country components
- Generates stacked bar chart comparing PIP vs WID breakdown
- Analysis was started but not yet completed at end of session

### Session 2 (Feb 7, 2026) - Per Capita vs Per Adult Correction

**Issue Identified:**
- WID data represents income **per adult** (household income divided by adult population)
- PIP data represents income **per capita** (divided by total population including children)
- This fundamental difference was not accounted for in initial analysis

**Files Modified:**
1. `harmonize_percentiles.py` - Updated to create both per adult and per capita versions of WID data
2. `analyze_inequality.py` - Updated to analyze both WID_per_adult and WID_per_capita separately
3. `comparison_plots.py` - Created script for comparing PIP vs WID mean incomes with interactive plots

**New Files Created:**
1. `modified/WID_percentiles_with_per_capita.csv` - WID data with additional per capita average column
2. Updated `modified/pip_wid_harmonized.csv` - Now includes three sources: PIP, WID_per_adult, WID_per_capita

**Key Changes:**
- Calculate WID per capita averages: avg_per_capita = (avg_per_adult * adult_pop) / total_pop
- WID_per_adult uses adult_pop for population figures
- WID_per_capita uses total_pop for population figures
- MLD analysis now compares all three: PIP, WID_per_adult, WID_per_capita
- Visualization shows three bars for complete comparison

### Session 2 Continuation (Feb 8, 2026) - Interactive Visualizations & Country-Level Analysis

**Files Created:**
1. `comparison_plots.py` - Interactive scatter plots comparing PIP vs WID mean incomes
2. `compare_country_mlds.py` - Country-level within-country MLD comparison

**Interactive Visualizations Developed:**

1. **Mean Income Comparison Plot** (`pip_vs_wid_mean_income_interactive.html`):
   - Compares PIP vs WID per capita mean incomes across 211 countries
   - Log-log scale with readable dollar tick marks ($1, $2, $5, $10, etc.)
   - Reference lines (1x, 2x, 3x, 5x, 7x, 10x, 20x) labeled directly on lines
   - Regression lines (weighted and unweighted by population)
   - Top 10 most populous countries highlighted and labeled
   - Interactive tooltips showing daily and annual income, ratios
   - Key finding: WID systematically reports ~2.78x higher mean incomes (median ratio: 2.47x)
   - Regression: Unweighted WID = 2.15 × PIP^1.05, Weighted WID = 3.68 × PIP^0.90

2. **Within-Country MLD Comparison Plot** (`pip_vs_wid_country_mlds.html`):
   - Compares within-country inequality (MLD) for each country
   - Reference lines (1x, 2x, 3x, 5x, 7x) with direct labels
   - Top 10 most populous countries highlighted
   - Interactive tooltips with MLD values and mean incomes
   - **Critical finding**: WID shows higher within-country inequality in 100% of countries (211/211)
   - Most countries cluster around 2x-3x lines
   - Latin American outliers (El Salvador, Mexico, Peru, Chile, Colombia) show 5x-7x ratios

**Key Insights:**

**MLD Decomposition Results (2023):**
- PIP (per capita): Total MLD = 0.6945, 69.2% between-country, 30.8% within-country
- WID (per adult): Total MLD = 0.9985, 28.3% between-country, 71.7% within-country
- WID (per capita): Total MLD = 1.1035, 35.7% between-country, 64.3% within-country

**Impact of Per Capita Adjustment:**
- Converting WID from per adult to per capita increases between-country share by 7.4 percentage points
- Gap between PIP and WID per capita remains substantial: 33.5 percentage points
- Average adjustment factor: adult/total population ratio ≈ 0.668

**Systematic Patterns:**
- WID consistently reports higher mean incomes than PIP (median: 2.47x, mean: 2.78x)
- WID consistently reports higher within-country inequality than PIP (100% of countries)
- Average within-country MLD: PIP = 0.247, WID = 0.730 (~3x higher)
- Most populous countries follow similar patterns (India, China, USA, Indonesia, Pakistan)
- Latin America shows exceptionally large WID-PIP gaps for within-country inequality

**Technical Notes:**
- All visualizations are interactive HTML using Plotly
- Reference lines labeled directly on plots (not in legends)
- Population-weighted and unweighted regressions both included where relevant
- Logged power law relationships for mean income (log-log regression)
- Linear relationships for MLD comparison

**Outputs:**
- `outputs/inequality_decomposition.png` - Three-bar MLD decomposition chart
- `outputs/pip_vs_wid_mean_income_interactive.html` - Interactive mean income scatter plot
- `outputs/pip_vs_wid_country_mlds.html` - Interactive within-country MLD scatter plot

### Session 2 Continuation (Feb 8, 2026) - Counterfactual Inequality Analysis

**File Created:**
- `counterfactual_analysis.py` - Swap inequality distributions between countries to assess sensitivity

**Methodology:**
Created a counterfactual analysis framework to understand how individual countries' inequality patterns affect global inequality decomposition. The approach:
1. Takes the inequality distribution (shape) from a source country
2. Rescales it to match the target country's mean income
3. Replaces the target country's actual distribution in the global data
4. Recalculates global MLD decomposition

This allows asking questions like "What if Mexico had the US's inequality distribution?" while preserving Mexico's mean income.

**Counterfactual Scenarios Analyzed:**

Three scenarios were run using WID per capita data:

1. **Mexico→US**: Replace Mexico's inequality with US pattern
   - Mexico: Population 130M, original MLD = 1.946
   - US: MLD = 0.741
   - Impact: +0.64 pp increase in between-country share (35.7% → 36.4%)
   - Total MLD: 1.1035 → 1.0842

2. **Brazil→US**: Replace Brazil's inequality with US pattern
   - Brazil: Population 211M, original MLD = 1.676
   - US: MLD = 0.741
   - Impact: +0.81 pp increase in between-country share (35.7% → 36.5%)
   - Total MLD: 1.1035 → 1.0791

3. **India→Pakistan**: Replace India's inequality with Pakistan's pattern
   - India: Population 1.44B, original MLD = 0.659
   - Pakistan: MLD = 0.418
   - Impact: **+1.44 pp** increase in between-country share (35.7% → 37.2%)
   - Total MLD: 1.1035 → 1.0608

**Key Findings:**

- **India has the largest global impact** due to its enormous population (1.44 billion)
  - Despite having moderate inequality (MLD=0.659), its size makes it globally influential
  - Reducing India's inequality to Pakistan's level shifts global decomposition by 1.44 pp

- **Latin American countries** (Mexico, Brazil) have very high inequality (MLD ~1.7-1.9)
  - But smaller populations limit their global impact
  - Brazil's larger population (211M vs 130M) gives it more influence than Mexico

- **All counterfactuals reduce global within-country inequality**
  - Demonstrates that high-inequality countries (especially populous ones) are key drivers
  - Reducing inequality in these countries would meaningfully shift global inequality toward between-country component

- **Population matters more than inequality level**
  - India's 1.44pp impact > Brazil's 0.81pp impact, despite Brazil having 2.5x higher inequality
  - Population weighting in MLD decomposition amplifies effects of populous countries

**Output:**
- `outputs/counterfactual_decomposition.png` - Four-bar chart showing original and three counterfactual scenarios

**Technical Implementation:**
- Function preserves target country's mean income and population structure
- Only changes the shape of within-country distribution
- Between-country component remains unchanged (as expected, since means are preserved)
- Validates that within-country component changes drive the results

### Session 3 (Feb 19, 2026) - Shape vs Mean Analysis

**File Created:**
- `shape_vs_mean_analysis.py` - Isolate whether PIP-WID gap is driven by mean incomes or distribution shapes

**Motivation:**
Previous analysis showed WID systematically reports 2.78x higher mean incomes than PIP (median: 2.47x), with population-weighted differences varying systematically between rich and poor countries. This raised the question: How much of the 33.5 percentage point gap in between-country share is due to different mean incomes versus different within-country inequality patterns?

**Methodology:**
Created two key counterfactuals by swapping distribution shapes between sources while preserving means:
1. **WID shapes with PIP means**: Take WID's percentile distribution for each country, rescale to match PIP's mean income
2. **PIP shapes with WID means**: Take PIP's percentile distribution for each country, rescale to match WID's mean income

This isolates the contribution of:
- Distribution shape differences (within-country inequality patterns)
- Mean income differences (systematic level differences between sources)

**Results:**

**Original Decompositions:**
- PIP: 69.2% between-country, 30.8% within-country (Total MLD = 0.6945)
- WID per capita: 35.7% between-country, 64.3% within-country (Total MLD = 1.1035)
- **Gap: 33.50 percentage points**

**Counterfactual 1: WID Shapes with PIP Means**
- Between-country: 40.4%, Within-country: 59.6% (Total MLD = 1.1903)
- Gap from WID original: **4.69 pp** (very close to WID)
- Gap from PIP original: 28.81 pp (far from PIP)
- **Interpretation**: Even with PIP's lower mean incomes, WID's distribution shapes preserve the low between-country share

**Counterfactual 2: PIP Shapes with WID Means**
- Between-country: 64.8%, Within-country: 35.2% (Total MLD = 0.6080)
- Gap from PIP original: **4.38 pp** (very close to PIP)
- Gap from WID original: 29.12 pp (far from WID)
- **Interpretation**: Even with WID's higher mean incomes, PIP's distribution shapes preserve the high between-country share

**Key Findings:**

**Distribution shapes drive ~87% of the gap:**
- Mean income differences contribute: ~4.5 pp (13% of 33.5 pp gap)
- Distribution shape differences contribute: ~29 pp (87% of 33.5 pp gap)
- **Conclusion**: The PIP-WID disagreement is fundamentally about different within-country inequality patterns, not different mean income levels

**Implications:**
- WID shows much higher within-country inequality than PIP (within-country MLD: 0.709 vs 0.214, 3.3x higher)
- This higher within-country inequality in WID is what drives its conclusion that most global inequality is within-country
- The systematic mean income differences (WID reporting 2.78x higher means) have only a minor effect on the decomposition
- Population-weighted variation in mean income ratios across rich/poor countries contributes minimally

**Output:**
- `outputs/shape_vs_mean_decomposition.png` - Four-bar comparison showing original PIP, original WID, and both counterfactuals

**Technical Notes:**
- Rescaling preserves country-specific means exactly (verified with spot checks)
- Uses 211 common countries present in both PIP and WID
- Maintains each source's population structure (important for per capita calculations)
- Within-country MLD preserved exactly when rescaling (only between-country component affected by mean changes)

### Session 4 (Feb 20, 2026) - Pipeline Reorganization & Multi-Income Analysis

**Project Restructuring:**

1. **Created Production Pipeline** (`run_all.py`, `config.py`)
   - Main orchestrator script with 8 pipeline steps
   - Centralized configuration in `config.py`
   - Color-coded logging and progress tracking
   - Command-line options: `--skip-stata`, `--step`, `--list`
   - Error handling with user prompts
   - Complete automation from data fetch to final outputs

2. **Robust WID Data Fetching** (`fetch_wid_data.py`)
   - **Problem**: WID API unreliable with large requests (all countries × all percentiles)
   - **Solution**: Country-by-country approach
     - Fetches PPP and population separately (fast)
     - Fetches percentile data one country at a time
     - Progress tracking with resume capability (`fetch_progress.json`)
     - Stores each country separately in `temp_country_data/`
     - Combines at the end into final file
   - **Benefits**: Fully resumable, handles API failures, clear progress tracking
   - Stata scripts: `fetch_ppp.do`, `fetch_population.do`
   - Temporary .do files generated on-the-fly for each country

3. **Multi-Income Concept Support**
   - Added **post-tax disposable income** to data fetching
   - Indicators fetched:
     - Pre-tax national income: `aptinc` (average), `sptinc` (share)
     - Post-tax disposable income: `adiinc` (average), `sdiinc` (share)
   - Graceful handling when income concepts missing for some countries
   - Uses `capture` in Stata to handle missing variables

**Files Created:**

1. `run_all.py` - Main pipeline orchestrator
2. `config.py` - Centralized configuration
3. `requirements.txt` - Python dependencies
4. `README.md` - Project documentation
5. `fetch_wid_data.py` - Robust WID fetching system
6. `inputs/fetch_ppp.do` - Fetch PPP data
7. `inputs/fetch_population.do` - Fetch population data
8. `WID_FETCHING.md` - Technical documentation for WID fetching
9. `create_wid_income_concepts.py` - Process multiple income concepts
10. `analyze_income_concepts.py` - Compare across income concepts

**Files Removed:**
- `inputs/fetch_percentiles_aggs_and_pop.do` (obsolete monolithic approach)

**Income Concept Analysis:**

Created comprehensive analysis comparing four data sources:
1. **PIP (per capita)** - Baseline
2. **WID Pre-tax (per adult)** - Original WID structure
3. **WID Pre-tax (per capita)** - Adjusted for per capita comparison
4. **WID Post-tax (per capita)** - Post-tax income with per capita adjustment

**Key Implementation Details:**
- WID data converted from annual to daily income (÷ 365) to match PIP
- Per capita adjustment: `(avg_per_adult × adult_pop) / total_pop`
- Both income concepts processed simultaneously
- Handles missing data gracefully (not all countries have post-tax)

**Testing Results (with 33 countries):**

| Dataset | Between-Country | Within-Country | Total MLD |
|---------|-----------------|----------------|-----------|
| PIP (per capita) | 69.2% | 30.8% | 0.6945 |
| WID Pre-tax (per adult) | 40.8% | 59.2% | 2.4962 |
| WID Pre-tax (per capita) | 39.6% | 60.4% | 2.4363 |
| WID Post-tax (per capita) | 44.4% | 55.6% | 2.2056 |

**Key Findings:**
- Post-tax income shows higher between-country share (44.4%) than pre-tax (39.6%)
- Post-tax has lower total MLD (2.21 vs 2.44) - less inequality overall
- Per capita adjustment increases between-country share by ~1 pp
- Pattern holds even with limited test data

**Outputs:**
- `outputs/income_concepts_decomposition.png` - Four-bar comparison chart

**Documentation Reorganization:**
- Renamed `CLAUDE.md` → `.claude.md` (hidden file, standard convention)
- `README.md` - Public-facing documentation
- `WID_FETCHING.md` - Technical deep-dive on data fetching
- `.claude.md` - Session history and development notes (this file)

**Pipeline Status:**
- Full WID data fetch running in background (~70 min for all countries)
- Test data (33 countries) confirms analysis pipeline works correctly
- Ready for production run with complete dataset

### Session 5 (Feb 20, 2026) - Critical PPP Conversion Bug Fix

**Major Bug Discovered:**

After completing the full WID data fetch (211 countries, 104.5 minutes), the income concept analysis showed **extremely high global mean incomes**:
- WID Pre-tax (per adult): $118,395/day (should be ~$80-90/day)
- Iran top 0.1%: $199.8 MILLION/day per person

**Root Cause:**

WID API returns income data in **local currency units** (e.g., Iranian Rials, US Dollars), NOT in PPP-adjusted international dollars. The processing scripts were failing to convert using PPP factors.

Investigation revealed:
- Iran raw data: 72,927,411,594 Rials/year (annual, local currency)
- Iran PPP conversion factor: 92,172 Rials per international dollar
- Without PPP conversion: 72.9 billion ÷ 365 = $199.8 million/day ❌
- With PPP conversion: 72.9 billion ÷ 92,172 ÷ 365 = $2,167.70/day ✓

The US (PPP=1) had correct values, masking the bug during initial testing.

**Files Modified:**

1. **`create_wid_income_concepts.py`**
   - Added PPP data loading and merge
   - Updated annual→daily conversion to include PPP:
     ```python
     # Before (WRONG):
     avg_pretax_daily = avg_pretax / 365

     # After (CORRECT):
     avg_pretax_daily = (avg_pretax / ppp) / 365
     ```
   - Added bin-specific population calculation:
     ```python
     bin_adult_pop = adult_pop × (p_high - p_low)
     bin_total_pop = total_pop × (p_high - p_low)
     ```

2. **`create_wid_per_capita.py`**
   - Applied same PPP conversion fix
   - Now loads from temp country files instead of combined file
   - Added bin-specific population columns

3. **`harmonize_percentiles.py`**
   - **Removed redundant PPP conversion** (data already converted in processing)
   - Updated to use pre-calculated `bin_adult_pop` and `bin_total_pop`
   - Simplified to just use already-converted values

4. **`analyze_income_concepts.py`**
   - Updated to use `bin_adult_pop` for per adult analysis
   - Updated to use `bin_total_pop` for per capita analysis

**Bin Population Bug Fix:**

Also discovered that WID data has the TOTAL country population repeated for every percentile row, not bin-specific populations. The MLD calculation was incorrectly multiplying each bin's average income by the entire country population.

Fixed by calculating: `bin_pop = total_pop × (p_high - p_low)`

**Corrected Results (211 countries, full dataset):**

| Dataset | Global Mean | Total MLD | Between | Within |
|---------|-------------|-----------|---------|--------|
| PIP (per capita) | $21.11/day | 0.69 | 69.2% | 30.8% |
| WID Pre-tax (per adult) | $89.96/day | 1.08 | 31.8% | 68.2% |
| WID Pre-tax (per capita) | $58.31/day | 1.21 | 39.1% | 60.9% |
| WID Post-tax (per capita) | $55.84/day | 1.01 | 47.3% | 52.7% |

**Verification:**
- US median (p50p51): $138.83/day = $50,673/year ✓
- Iran top 0.1%: $2,167.70/day = $791,211/year ✓
- All global means now reasonable and consistent

**Impact:**

This was a **critical bug** that would have completely invalidated all WID-based analyses. The fix ensures:
1. Proper currency conversion from local to international dollars
2. Correct population weighting in MLD calculations
3. Accurate global mean income estimates
4. Valid inequality decomposition results

All downstream analyses (`analyze_inequality.py`, `comparison_plots.py`, `counterfactual_analysis.py`, `shape_vs_mean_analysis.py`) now produce correct results.

**Testing:**
- Regenerated all WID processed files
- Ran full harmonization pipeline
- Verified all analysis scripts produce reasonable outputs
- All visualizations updated with corrected data

### Session 6 (Feb 20, 2026) - Zero Income Handling & Code Reorganization

**Issue Discovered: Inconsistent Zero Handling**

When building the slide deck, noticed that three different charts showed slightly different MLD values for the same dataset (WID per adult):
- `basic_decomposition.png`: 28.0% between, MLD = 0.9949
- `income_concepts_decomposition.png`: 28.0% between, MLD = 0.9949
- `inequality_decomposition.png`: 29.7% between, MLD = 0.9501

**Root Causes Identified:**

1. **Duplicate MLD calculation code**: Each analysis script had its own copy of the MLD decomposition function, creating maintenance burden and inconsistency risk.

2. **Inconsistent zero handling**: Different scripts filtered zero-income rows at different stages:
   - Some filtered zeros globally before any calculation
   - Some filtered zeros per-country when calculating within-country MLD
   - This caused different effective populations and different results

3. **Country mapping error**: Guernsey (GG) was incorrectly mapped to "Germany" instead of "Channel Islands" in `country_mapping.csv`, causing harmonized data to have 210 countries instead of 211.

**Why Zero Incomes Are Problematic:**

The Mean Log Deviation (MLD) formula is:

```
MLD = Σ (pop_i / total_pop) × ln(mean / income_i)
```

When `income_i = 0`, we have `ln(mean/0) = ln(∞)` which is mathematically undefined. The logarithm cannot handle zero or negative values.

**Why Zeros Exist in the Data:**

Both PIP and WID have percentile bins with zero reported income (921 out of 22,999 rows in WID). These appear primarily in the lowest income bins and could represent:
- Data collection limitations or reporting thresholds
- People with genuinely zero reported income in surveys
- Very small values rounded down to zero
- Missing data coded as zero

**Previous Approach (Problematic):**

Scripts were **filtering out zero-income rows** entirely. This approach:
- Reduces the effective population being analyzed
- Can bias results by excluding the poorest segments
- Creates inconsistencies when filtering happens at different stages
- Makes it difficult to compare results across different decompositions

**New Approach: Replace Zeros with $0.01/day**

Instead of filtering, replace zero incomes with **1 cent per day ($0.01)**:

**Rationale:**
1. **Mathematically valid**: Allows MLD calculation to proceed (ln(mean/0.01) is defined)
2. **Minimal impact on results**: $0.01 is so small that it barely affects inequality measures
3. **Inclusive**: Retains all people in the analysis with their correct population weights
4. **Conservative**: Treats zero-income as "extremely low" rather than filtering out entirely
5. **Consistent**: Same approach applied uniformly across all calculations

**Code Reorganization:**

Created `mld_decomposition.py` - a shared module containing:
- Single authoritative MLD calculation function
- Zero-to-one-cent replacement logic
- Consistent handling of edge cases
- All analysis scripts import from this module

**Files Modified:**

1. **`mld_decomposition.py`** (NEW)
   - Single source of truth for MLD calculations
   - Replaces zeros with $0.01 before any calculation
   - Returns standardized results dictionary

2. **`inputs/country_mapping.csv`**
   - Fixed: `GG,Guernsey,Channel Islands` (was incorrectly "Germany")

3. **All analysis scripts updated to use shared function:**
   - `create_basic_decomposition.py`
   - `analyze_income_concepts.py`
   - `analyze_inequality.py`
   - `comparison_plots.py`
   - `compare_country_mlds.py`

**Results After Fix:**

All three charts now show consistent values:
- WID (per adult): 211 countries, consistent MLD decomposition
- China data now included with correct structure (pre-tax and post-tax)
- Guernsey correctly mapped to Channel Islands

**Impact of Zero Replacement:**

The $0.01 replacement has minimal impact on results because:
- Zero values are rare (921/22,999 rows = 4%)
- They occur in low-population bins
- $0.01 is orders of magnitude smaller than typical incomes
- Results change by <0.1 percentage points

### Data Validation System Added

**Motivation:**

After discovering inconsistencies in MLD calculations due to zero handling, concerns arose about other potential data quality issues throughout the processing pipeline. Given that WID data fetching takes 100+ minutes, it's important to validate data before attempting time-consuming re-fetches.

**New Validation System in `fetch_wid_data.py`:**

Added comprehensive validation checks that run **before** any data fetching:

1. **File Existence Check**: Verifies all expected country files are present
2. **Column Structure Check**: Ensures files have correct columns (country, percentile, year, avg_pretax, avg_posttax, share_pretax, share_posttax, p_low, p_high)
3. **Row Count Check**: Verifies each country has exactly 109 percentile rows
4. **NA/Missing Data Check**: Identifies completely empty rows and NA values in key columns
5. **Data Validity Check**: Ensures percentile bounds are valid (0-1 range, p_low < p_high)
6. **Year Check**: Confirms all data is for the expected year (2023)

**New Workflow:**

```bash
# Default: validate first, then ask for confirmation
python fetch_wid_data.py

# Only run validation, don't fetch
python fetch_wid_data.py --validate-only

# Skip validation and fetch directly
python fetch_wid_data.py --skip-validation
```

**Sample Output:**

```
======================================================================
WID DATA VALIDATION REPORT
======================================================================

Total countries checked: 215
✓ Valid files: 211
✗ Files with issues: 4

❌ MISSING FILES (4 countries):
   AI (Anguilla)
   BQ (Bonaire)
   JE (Jersey)
   MS (Montserrat)

======================================================================

⚠️  You are about to run a full data fetch. This typically takes 100+ minutes.
⚠️  4 countries have data quality issues that will be addressed.
Do you want to proceed with fetching? [y/N]:
```

**Benefits:**

1. **Prevents unnecessary re-fetches**: Quickly check data quality before committing to 100+ minute fetch
2. **Identifies specific problems**: Pinpoints exactly which countries have issues
3. **Saves time**: Running validation takes ~30 seconds vs 100+ minutes for full fetch
4. **Increases confidence**: Systematic checks catch structural issues early
5. **User control**: Explicit confirmation required before long-running operations

**Validation Categories:**

- Missing files: Countries where CSV file doesn't exist
- Wrong column structure: Files missing expected columns
- Wrong row count: Files with ≠ 109 rows
- NA/missing data: Empty rows or NA values in required columns
- Other issues: Invalid percentile bounds, wrong year, etc.

### Slide Deck and Project Reorganization

**Slide Deck Framework Created:**

Created `outputs/inequality_narrative.html` - A reveal.js-based interactive presentation to organize findings:
- Single HTML file approach (no dependencies)
- Two-thirds layout (image left, text right)
- Shows both absolute MLD values and percentages in bars
- Interactive keyboard navigation
- First slide: Basic PIP vs WID (per adult) comparison

**Final Request - Project Reorganization:**

User requested tidying up the codebase to separate calculation from visualization:

1. **Remove unnecessary scripts**: Many scripts created for checking/validation during debugging are no longer needed
2. **Separate analysis from plotting**:
   - Create single `calculate_all_decompositions.py` that runs ALL decomposition calculations
   - Save results to CSV with verbose labels (`outputs/mld_decompositions.csv`)
   - Create separate plotting scripts that read from this CSV
3. **Benefits**:
   - Single source of truth for calculations
   - Easy to regenerate plots without recalculating
   - Clear separation of concerns
   - Verbose CSV makes it easy to understand which analysis is which

**Files to Create:**
- `calculate_all_decompositions.py` - Calculate all decompositions, save to CSV
- `plot_basic_decomposition.py` - Read CSV, generate first slide chart
- `plot_income_concepts.py` - Read CSV, generate 4-bar comparison
- Other plotting scripts as needed

**Scripts to Remove:**
- `check_country_columns.py` (one-time debugging)
- `refetch_countries.py` (one-time fixing)
- Other temporary validation scripts

**Completed Reorganization:**

1. ✅ **Created `calculate_all_decompositions.py`**
   - Calculates all MLD decompositions in one script
   - Saves to `outputs/mld_decompositions.csv` with verbose labels
   - 4 analyses: PIP per capita, WID pre-tax per adult, WID pre-tax per capita, WID post-tax per capita
   - Clear column names: analysis_id, dataset, income_concept, population_basis, etc.

2. ✅ **Created `plot_all_decompositions.py`**
   - Single script that reads from CSV and generates all visualizations
   - Three functions: plot_basic_decomposition(), plot_income_concepts(), plot_inequality_comparison()
   - Generates all three charts needed for narrative presentation

3. ✅ **Removed obsolete scripts:**
   - `check_country_columns.py` (one-time debugging)
   - `refetch_countries.py` (one-time fixing)
   - `create_basic_decomposition.py` (replaced by plot_all_decompositions.py)
   - `analyze_inequality.py` (replaced by calculate_all_decompositions.py)
   - `analyze_income_concepts.py` (replaced by calculate_all_decompositions.py)

**Current Script Organization:**

**Data Processing:**
- `fetch_wid_data.py` - Fetch WID data from API (with validation)
- `create_wid_income_concepts.py` - Process WID income concepts
- `create_wid_per_capita.py` - Convert WID to per capita
- `harmonize_percentiles.py` - Harmonize PIP and WID data

**Analysis:**
- `calculate_all_decompositions.py` - Calculate all MLD decompositions → CSV
- `compare_country_mlds.py` - Country-level within-country MLD comparison
- `counterfactual_analysis.py` - Swap inequality distributions between countries
- `shape_vs_mean_analysis.py` - Isolate shape vs mean effects

**Visualization:**
- `plot_all_decompositions.py` - Generate all stacked bar charts from CSV
- `comparison_plots.py` - Interactive scatter plots (mean income, country MLDs)

**Utilities:**
- `mld_decomposition.py` - Shared MLD calculation module
- `config.py` - Configuration settings
- `run_all.py` - Main pipeline orchestrator

**Benefits of New Organization:**
- Single source of truth for calculations (CSV)
- Easy to regenerate plots without recalculating
- Clear separation: data processing → analysis → visualization
- No duplicate analysis code
- Easier to maintain and extend

### Session 7 (Mar 2, 2026) - Project Reorganization Completion

**Context:** This session continued from Session 6 where reorganization was requested but not completed due to running out of context.

**Tasks Completed:**

1. **Completed `calculate_all_decompositions.py`**
   - Finished implementation from Session 6
   - Successfully ran script to generate `outputs/mld_decompositions.csv`
   - CSV contains 4 decomposition analyses with verbose labels
   - Results table shows consistent values across all analyses

2. **Created `plot_all_decompositions.py`** (single unified plotting script)
   - User requested all plots in one script (not separate files)
   - Three plotting functions:
     - `plot_basic_decomposition()` - PIP vs WID per adult (2 bars)
     - `plot_income_concepts()` - All 4 income concepts (4 bars)
     - `plot_inequality_comparison()` - Full comparison (4 bars)
   - All plots read from CSV, no recalculation needed
   - Successfully generated all three PNG files

3. **Cleaned Up Obsolete Scripts**
   - Removed 5 temporary/duplicate scripts:
     - `check_country_columns.py` - one-time debugging tool
     - `refetch_countries.py` - one-time data fixing tool
     - `create_basic_decomposition.py` - replaced by unified plotting script
     - `analyze_inequality.py` - replaced by calculate_all_decompositions.py
     - `analyze_income_concepts.py` - replaced by calculate_all_decompositions.py

4. **Updated Documentation**
   - Documented completed reorganization in .claude.md
   - Listed current script organization by category
   - Explained benefits of new structure

**Current State:**

The project now has a clean, maintainable structure:
- **Single analysis script** calculates all decompositions → saves to CSV
- **Single plotting script** reads CSV → generates all visualizations
- **No code duplication** - all MLD calculations use shared `mld_decomposition.py`
- **Clear workflow**: Data processing → Analysis (CSV) → Visualization (PNG)

**Results Verified:**

All decomposition values are now consistent across all charts:
- PIP per capita: 69.2% between, 30.8% within (MLD = 0.6945)
- WID pre-tax per adult: 21.7% between, 78.3% within (MLD = 1.3011)
- WID pre-tax per capita: 28.3% between, 71.7% within (MLD = 1.3928)
- WID post-tax per capita: 42.4% between, 57.6% within (MLD = 0.9300)

**Files Created/Modified:**
- ✅ `calculate_all_decompositions.py` - Completed implementation
- ✅ `plot_all_decompositions.py` - Created unified plotting script
- ✅ `outputs/mld_decompositions.csv` - Generated with all results
- ✅ All three PNG charts regenerated with consistent values
- ✅ `.claude.md` - Updated with Session 7 notes

**Session Status:** Complete - Project reorganization successfully finished.


